{
  "name": "borrow",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "react-devtool": "react-devtools",
    "clean": "rm -rf node_modules/ && rm -rf $TMPDIR/react-* && rm -rf $TMPDIR/metro-* && rm -rf $TMPDIR/haste-* && rm -rf ios/build && rm -rf android/build && rm -rf android/app/build && rm -f yarn.lock && yarn cache clean && yarn install",
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest"
  },
  "dependencies": {
    "eslint": "^5.2.0",
    "react": "16.3.1",
    "react-devtools": "^3.2.3",
    "react-native": "0.55.4",
    "react-native-vector-icons": "^5.0.0"
  },
  "devDependencies": {
    "babel-jest": "23.4.0",
    "babel-preset-react-native": "4.0.0",
    "eslint-config-rallycoding": "^3.2.0",
    "jest": "23.4.1",
    "react-test-renderer": "16.3.1"
  },
  "jest": {
    "preset": "react-native"
  }
}
